<%= form_for(prepared) do |f| %>
  <% if prepared.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prepared.errors.count, "error") %> prohibited this prepared from being saved:</h2>

      <ul>
      <% prepared.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :button %>
    <%= f.text_field :button %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
